<UserControl
    x:Class="DEDSEC.WPF.Components.AddGameComponent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Width="400" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            FontSize="28"
            Text="Добавить игру" />

        <Grid
            Grid.Row="1"
            Margin="0,20,0,0"
            Grid.IsSharedSizeScope="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" SharedSizeGroup="Title" />
                    <ColumnDefinition Width="auto" SharedSizeGroup="Value" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Название" />
                <TextBox Grid.Column="1" Text="{Binding Name}" />
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" SharedSizeGroup="Title" />
                    <ColumnDefinition Width="auto" SharedSizeGroup="Value" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Описание" />
                <TextBox Grid.Column="1" Text="{Binding Description}" />
            </Grid>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" SharedSizeGroup="Title" />
                    <ColumnDefinition Width="auto" SharedSizeGroup="Value" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Минимум игроков" />
                <TextBox Grid.Column="1" Text="{Binding MinCountPlayers}" />
            </Grid>
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" SharedSizeGroup="Title" />
                    <ColumnDefinition Width="auto" SharedSizeGroup="Value" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Максимум игроков" />
                <TextBox Grid.Column="1" Text="{Binding MaxCountPlayers}" />
            </Grid>
            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" SharedSizeGroup="Title" />
                    <ColumnDefinition Width="auto" SharedSizeGroup="Value" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Ссылка на HobbyGames" />
                <TextBox Grid.Column="1" Text="{Binding LinkHobbyGames}" />
            </Grid>
        </Grid>

        <StackPanel
            Grid.Row="2"
            Margin="0,20,0,0"
            Orientation="Horizontal">
            <Button
                Padding="10,5"
                Command="{Binding SubmitCommand}"
                Content="Submit" />
            <Button
                Margin="5,0,0,0"
                Padding="10,5"
                Command="{Binding CancelCommand}"
                Content="Cancel" />
        </StackPanel>
    </Grid>
</UserControl>
